Installation:
	
# Clone the repository
> git clone git://github.com/jmaicher/env.git ~/.env

# Init/update git submodules
> git submodule init && git submodule update

# Create symlinks
> ln -s ~/.env/.tmux.conf ~/.tmux.conf
> ln -s ~/.env/.vim/.vimrc ~/.vimrc
> ln -s ~/.env/.vim ~/.vim
